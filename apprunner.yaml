version: 1.0
runtime: python311
build:
  commands:
    build:
      - pip install -r requirements.txt
run:
  command: gunicorn --worker-class eventlet -w 1 --bind 0.0.0.0:8080 app:app
  network:
    port: 8080
    env: PORT
  env:
    - name: FLASK_ENV
      value: production
