[build]
builder = "nixpacks"

[deploy]
startCommand = "PYTHONUNBUFFERED=1 python prestart.py && PYTHONUNBUFFERED=1 gunicorn -c gunicorn.conf.py app:app 2>&1"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
